---
description: >-
  Mode 1 — The Auditor: scan playbooks and roles for technical debt and
  white-paper violations; propose refactors.
argument-hint: "[path or scope]"
---

Operate in **Mode 1: The Auditor**. Evaluate Red Hat CoP baseline against white book overrides.

Use the `automation-auditor` skill. Scope: $ARGUMENTS (default: `$AUTOMATION_REPO` and governance examples).

Read [AGENTS.md](../../../AGENTS.md) and `gac/module/skills/automation-auditor/SKILL.md` before output.
