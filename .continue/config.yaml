# Continue.dev — local Ollama in Dev Spaces (same pod as the IDE).
# Copied to /home/user/.continue/ on workspace postStart (.devfile/setup-workspace.sh).
# Ollama sidecar: qwen2.5-coder:7b (CPU, no GPU). API: http://127.0.0.1:11434
#
name: Automation Home (Ollama)
version: "1.0.0"
schema: v1

models:
  - name: Qwen2.5 Coder 7B
    provider: ollama
    model: qwen2.5-coder:7b
    apiBase: http://127.0.0.1:11434
    roles:
      - chat
      - edit
      - apply
      - autocomplete

tabAutocompleteModel:
  name: Qwen2.5 Coder 7B
  provider: ollama
  model: qwen2.5-coder:7b
  apiBase: http://127.0.0.1:11434
