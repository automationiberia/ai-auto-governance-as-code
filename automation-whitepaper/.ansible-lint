---
# Department ansible-lint profile (used by pre-commit and CI)
profile: production
write_list:
  - none

exclude_paths:
  - ../automation-good-practices/
  - ../deliveries/

skip_list:
  - schema[version]  # allow collections without galaxy version in samples
