---
# Legacy optional devfile — UDI only, external LLM via secrets.
# Prefer the default .devfile.yaml (Automation Home):
# Copilot + Continue + Ollama sidecar.
# Use this file only with: ?devfilePath=.devfile/continue.yaml
schemaVersion: 2.3.0
metadata:
  name: vscode-continue-ai-env
  displayName: Continue.dev AI (optional)
  description: >-
    VS Code environment with Continue.dev AI assistant. Requires cluster secrets
    for CONTINUE_MODEL_* and CONTEXT7_API_KEY (mount-as env).
components:
  - name: vscode-editor
    container:
      image: quay.io/devfile/universal-developer-image:latest
      mountSources: true
commands:
  - id: copy-continue-dir
    exec:
      component: vscode-editor
      commandLine: |
        sh -c "
        echo '[postStart] Copying .continue to ~/.continue (env placeholders)...'
        mkdir -p /home/user/.continue
        dst=/home/user/.continue
        src=\"$PROJECT_SOURCE/.continue\"
        cp -r \"$src/\"* \"$dst/\"
        name=\"$CONTINUE_MODEL_NAME\"
        key=\"$CONTINUE_MODEL_API_KEY\"
        url=\"$CONTINUE_MODEL_API_URL\"
        ctx7key=\"$CONTEXT7_API_KEY\"
        awk -v n=\"\$name\" -v k=\"\$key\" -v u=\"\$url\" '
          BEGIN {
            gsub(/\\\\/, \"\\\\\\\\\", n); gsub(/&/, \"\\\\&\", n)
            gsub(/\\\\/, \"\\\\\\\\\", k); gsub(/&/, \"\\\\&\", k)
            gsub(/\\\\/, \"\\\\\\\\\", u); gsub(/&/, \"\\\\&\", u)
          }
          {
            gsub(/\\\$\{CONTINUE_MODEL_NAME\}/, n)
            gsub(/\\\$\{CONTINUE_MODEL_API_KEY\}/, k)
            gsub(/\\\$\{CONTINUE_MODEL_API_URL\}/, u)
            print
          }
        ' \"$src/config.yaml\" > \"$dst/config.yaml\"
        awk -v c7=\"\$ctx7key\" '
          BEGIN {
            gsub(/\\\\/, \"\\\\\\\\\", c7)
            gsub(/\"/, \"\\\\\\\"\", c7)
          }
          { gsub(/\\\$\{CONTEXT7_API_KEY\}/, c7); print }
        ' \"$src/mcpServers/mcp.json\" > \"$dst/mcpServers/mcp.json\"
        echo '[postStart] Done: .continue copied; Continue and MCP env resolved'
        "
      workingDir: ${PROJECT_SOURCE}
      label: Copy Continue config to home
  - id: install-continue-cli
    exec:
      component: vscode-editor
      commandLine: >-
        sh -c "echo '[postStart] Installing Continue CLI...' &&
        npm i -g @continuedev/cli &&
        echo '[postStart] Done: Continue CLI installed'"
      label: Install Continue CLI
events:
  postStart:
    - copy-continue-dir
    - install-continue-cli
